package functionality;

public class Board {

	private Bloc[][] board;

	public Board(int rows, int columns) {
		for (int i = 0; i < rows; i++)
			for (int j = 0; j < columns; j++)
				board[i][j] = new Bloc(j * 35 + 1, i * 35 + 1, 35);
	}
}
